# 사전등록 수정안 2종: E-B v1.0 및 E-A 부칙 A1
- 작성: 외부 감사자 (Claude, System 2), 2026-08-11
- 승인: 허상훈 (depth-margin 0.5 k_BT는 기승인; 본 문서로 문서화·봉인)
- 봉인: 본 문서 SHA-256 산출 → manifest 기입 → 이후 해당 스크립트 수정 개시

=============================================================================
# PART 1 — 수정안 E-B v1.0: η/σ 강건성 sweep + depth-margin 재채점
=============================================================================

## 1. 목적
논문 v4 Appendix D의 E-B 사양을 실행 프로토콜로 고정한다. 대상 주장:
게이트 결과(R1–R3)와 attractor 구조가 η·σ 선택에 강건함 (D8' 방어).

## 2. 확정 상수 (실행 후 변경 금지)
- 시드: {7777, 1234, 2026, 31415, 65537} (사전등록 verbatim)
- σ ∈ {0.05, 0.10, 0.15, 0.25} rad
- η ∈ {0.05, 0.10, 0.20, 0.50, 0.90}
- 학습 예산: 3000 Adam steps, batch 4096 (본선과 동일 — 축소 금지)
- depth-margin 임계값: 0.5 k_BT [PI 승인값]
- 게이트 상수: 기존 봉인본 verbatim (anchors, ±25°, R3 마진 0.05 k_BT)

## 3. stdout 필수 산출 (누락 시 해당 런 채택 불가)
(a) 상수 echo: 위 2절 전체 (특히 steps=3000, batch=4096 — 예산 검증용)
(b) 조건별 attractor 목록: (φ, ψ, 유입 초기점 수 n, macrostate, V)
(c) σ별 ΔF(α_R−β): 시드별 값과 시드 간 [min, max] 브래킷
(d) depth-margin 재채점: 각 attractor에 대해 |V − V_global_min| 산출,
    0.5 k_BT 기준으로 state/artifact 재분류, 기존 영역 기준 분류와의
    일치 여부를 attractor 단위로 표기
(e) 발산 케이스: 성공과 동등 비중으로 출력 (0건이어도 "Total: 0" 명기)
(f) 환경 기록 + 스크립트 self SHA-256 (64자)
(g) 조건별 wall-clock 시간 (예산 실적용 검증 보조)

## 4. 판정 규칙 (사전 고정)
- 강건성 인정: 전 (시드,σ,η) 조합에서 R1–R3 통과 그리고 σ별 ΔF 브래킷이
  본선 브래킷(0.54–0.84 k_BT)과 겹침.
- 부분 강건성: 일부 조합 실패 → 실패 조합을 표로 전부 보고하고 본문에
  한계로 서술 (은폐 없이 채택).
- depth-margin 재채점이 기존 분류(4 물리 + 1 아티팩트)와 다르면:
  depth-margin 판정을 채택하고 Table 2 갱신 + D10' 개정 (v5 Block 3
  Variant B 문안 사용).

## 5. 금지
- 시드·격자·예산·임계값의 코드-문서 불일치.
- 실행 후 임계값 변경. 판정 언어 출력.

=============================================================================
# PART 2 — 수정안 E-A 부칙 A1: 결함 시정 및 자기일관성 시험
=============================================================================

## 1. 배경 (사실 기록)
E-A 1차 봉인 실행(커밋 9c7405fc)의 stdout에서 다음이 확인됨:
P1/P2 attractor 전원이 ψ≈−4° 단일 골에 퇴화; β 미발견으로 R1–R3 전부
False; dF가 센티널(−999) 오염값으로 산출; 지표 (a)=0.101540은 두 게이지의
V(α_R) 원시값 차이로서 projection 의존성의 측정치가 아님. 본 부칙은 해당
결함을 시정하고 무의미 수치의 생성 경로를 차단한다.

## 2. 신설: 자기일관성 단위시험 V0 (선행 관문)
- 입력: EquiPhase 본선 체크포인트(seed 7777)의 보존 필드 S₀ = −∇V_θ.
- 절차: S₀를 P1, P2 각각에 투영해 Ṽ₀ 복원. 곡률 없는(curl=0) 필드이므로
  Ṽ₀는 V_θ와 상수 차이만 나야 함.
- 합격 기준 [사전 고정]: 평균 0 정규화 후 격자 전체
  corr(Ṽ₀, V_θ) ≥ 0.999 그리고 mean|Ṽ₀ − V_θ| ≤ 0.01 k_BT, P1·P2 모두.
- V0 불합격 상태에서 vanilla 필드 평가 단계로 진행 금지 (스크립트가
  V0 실패 시 abort 하도록 구현).

## 3. 개정: dF 및 지표 정의
- V(β) 또는 V(α_R)가 미발견이면 해당 게이지의 dF는 "undefined"로
  출력한다. 센티널 수치 대입 금지.
- 지표 (a)는 양쪽 dF가 모두 정의된 경우에만 산출. 아니면 "(a) undefined
  — reason: missing state(s)" 를 출력.
- 지표 (b)(c)는 기존 정의 유지.

## 4. 점검 의무 (봉인 전 코드에 반영)
meshgrid indexing('ij') 일관성; [−π,π) 도메인 파수 2π 계수; S=−∇V 부호;
체크포인트 임베딩 순서 [sinφ,cosφ,sinψ,cosψ]; 로드 직후 필드 3점 이상을
기존 sealed 평가값과 대조 출력.

## 5. 판정 규칙 재확인
v1.0 5절의 해석 A/B 및 임계 0.10 k_BT는 유지하되, 오직 V0 합격 +
dF 양측 정의 상태에서 산출된 (a)에만 적용된다. 그 외 산출물은 어떤
해석에도 사용 불가.

## 6. 기록
1차 실행(0.101540)은 폐기하지 않고 결함 실행으로 격리 보존한다
(EA_defective_run1/). 본 부칙 자체가 감사 원장 INC-33 후보(E-A 구현
결함: 퇴화 투영 및 센티널 dF)의 시정 조치 문서를 겸한다.
