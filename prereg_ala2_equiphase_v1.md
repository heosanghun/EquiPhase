# 사전등록 문서 v1 — EquiPhase DEQ × Alanine Dipeptide 실데이터 검증

- 작성: 외부 감사자 Claude (System 2) | 2026-08-08
- 지위: 본 문서 확정(해시 봉인 + 저장소 커밋) 후에만 훈련·게이트 실행 허용.
  본 문서에 없는 수치·주장·게이트는 논문 진입 금지 (FILL 원칙).
- 결정 위임 기록: 허상훈 님이 2026-08-08 대기 결정 일체를 감사자에게 명시
  위임. 이에 따라 입력 공간 A안(2D 이면각), 전략 (가)(단일 ICLR 논문),
  b-1 추인이 감사자 결정으로 확정됨.

## 1. 가설 (사전 고정)

- H1: 구조 보장(주기성 내장) + damped velocity Verlet 동역학을 갖는
  EquiPhase DEQ는 alanine dipeptide 이면각 데이터로부터 학습된 V에서
  두 개 이상의 물리적 준안정 상태(beta/PPII 분지, alpha_R 분지)를
  구별되는 끌개로 표현한다.
- H2: 동일 데이터·동일 예산에서 Monotone DEQ는 단일 끌개로 붕괴(N=1)하고,
  Vanilla DEQ는 끌개 위치 비정합 또는 비수렴을 보인다 (토이 결과의
  실데이터 재현).
- H3 (탐색적): 희소 상태 alpha_L (phi>0, 데이터 점유 2.998%)의 검출은
  탐색 과제로 두며, 성공·실패 어느 쪽도 H1·H2 판정에 영향을 주지 않는다.

## 2. 데이터 (변경 불가 앵커)

- 파일: data/ala2/alanine-dipeptide-3x250ns-backbone-dihedrals.npz
- SHA-256: F5AD30768A7CF3451B3061CB2ECB7F7D1DE8C13044534376D26A6653D4CD5717
- 구성: arr_0/1/2 각 (250000, 2), 총 750,000 프레임, 라디안.
- EDA 정본(봉인 스크립트 1bd99149…8618, 2026-08-08 실행)의 확정 사실:
  - fraction_phi_positive = 0.029977
  - MODE 1 (beta): (-72.50, +152.50) deg, box_pop 0.239521, F/kT 0.0000
  - MODE 2 (C5):   (-142.50, +157.50) deg, box_pop 0.150896, F/kT 0.4844
  - MODE 3 (aR):   (-72.50, -17.50) deg,  box_pop 0.113364, F/kT 0.7094
  - MODE 4:        (-127.50, +2.50) deg,  box_pop 0.054785, F/kT 1.7916
- 거시상태 정의 (사전 고정): beta 분지 = {psi in [+90, +180] ∪ [-180, -150]
  deg, phi<0}, alpha_R 분지 = {psi in [-90, +45] deg, phi<0},
  alpha_L = {phi>0}. 분지 앵커점: beta (-72.5, +152.5), aR (-72.5, -17.5).

## 3. 모델 (사전 고정)

- 주 실험 (A안): 입력 (phi, psi)를 [sin phi, cos phi, sin psi, cos psi]로
  인코딩 → V의 주기성이 구조적으로 보장됨 (토이의 G1/G2' 구조층에 대응).
- V_total = v_net (잔차 MLP). 해석적 V_base 부재는 공개조항 D2'로 기재.
- 동역학: damped velocity Verlet (토이와 동일 적분자, 명칭 규정 승계).
- 훈련: force matching 또는 score 기반 목적함수 중 봉인 게이트 스크립트에
  명기된 것 하나만 사용. 시드: 주 시드 7777 + 추가 시드 4개(사전 고정:
  1234, 2026, 31415, 65537). 하이퍼파라미터는 봉인 스크립트에 동결.
- 탐색 실험 (B안, 선택): 고차원 좌표 입력은 시간 잔여 시에만, 별도 사전
  등록 추가본으로.

## 4. 게이트 (사전 고정 — 판정은 감사자 봉인 스크립트 stdout만 유효)

- R1 (필수): 학습된 V에서 결정론적 끌개 탐색(다중 초기점 하강, 봉인
  스크립트 정의) 결과 서로 다른 거시상태(beta, alpha_R)에 각 1개 이상의
  끌개가 존재. N_basins >= 2.
- R2 (필수): 위 두 끌개의 위치가 분지 앵커점 대비 각각 ±25 deg 이내
  (phi, psi 각 축, 주기 거리).
- R3 (필수): 모델의 분지 상대 깊이/점유 순서가 데이터와 일치:
  F_model(beta) < F_model(alpha_R). 동률 판정 여유: 차이 >= 0.05 kT.
- R4 (필수): 동일 데이터·동일 예산에서 (a) Monotone DEQ N_basins = 1,
  (b) Vanilla DEQ가 R1∧R2를 동시 충족하지 못함.
- R5 (탐색적, 필수 아님): alpha_L (phi>0) 영역 끌개 검출. 결과와 무관하게
  보고 의무만 있음.
- 성공 정의: R1∧R2∧R3∧R4 전부 PASS. 하나라도 FAIL이면 본 가설은 해당
  형태로 기각되며, 결과는 있는 그대로 보고한다 (선택적 보고 금지).

## 5. 공개조항 (논문 동반 의무)

- D1': collective variable (phi, psi)은 모델에 제공됨 — 상태 발견이 아닌
  다중 안정성 표현 능력의 검증임.
- D2': 해석적 prior V_base 부재 — 토이의 3층 서술 중 'Prior 유래' 층은
  본 실험에 부분 적용.
- D3': EDA의 1D phi 장벽 프록시는 beta 분지 내부 기복을 측정한 것으로
  분지 간 장벽이 아님 — 장벽 정량 주장 금지, 순서 관계(R3)만 주장.
- D4': 단일 분자·단일 데이터셋 — 일반화 주장 금지.
- D5': R5 결과 (성공/실패 무관 보고).
- 토이 파트의 기존 6대 공개조항(D1~D6)은 그대로 승계.

## 6. 채널 규칙 (승계)

- 신규 수치는 감사자 저작·해시 봉인 스크립트의 직접 호출 stdout 원문만
  유효. 래퍼 무효. 훈련 스크립트는 안티그래비티가 본 문서 3절 사양대로
  작성 가능하나, 게이트 판정 수치는 봉인 스크립트만 산출한다.
- 증거 파일 덮어쓰기 금지, 실행마다 새 파일명. 절대 수식어 자기 보고
  금지. 실행하면 제출한다.

## 7. 일정·낙하 지점 (사전 고정)

- ICLR 2027 본문 마감 2026-09-25 AoE. R1~R4 미충족 또는 일정 미달 시
  낙하 지점은 TMLR (마감 없음)로 사전 고정 — 결과 왜곡 압력 제거 목적.

<!-- END OF PREREG_ALA2_EQUIPHASE_v1 -->
